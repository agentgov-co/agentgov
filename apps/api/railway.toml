[build]
builder = "dockerfile"
dockerfilePath = "../../Dockerfile"

[deploy]
startCommand = "pnpm --filter @agentgov/api start:migrate"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
