[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm build:api"

[deploy]
startCommand = "pnpm --filter @agentgov/api start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3