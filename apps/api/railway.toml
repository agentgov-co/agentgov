[build]
builder = "dockerfile"
dockerfilePath = "../../Dockerfile"

[deploy]
preDeployCommand = "cd /app/apps/api && npx prisma migrate deploy || npx prisma db push"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
