// API URL for backend
export const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3001'

// Local storage keys
export const STORAGE_KEYS = {
  SELECTED_PROJECT: 'agentgov_selected_project',
} as const
