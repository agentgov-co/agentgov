# === Environment ===
NODE_ENV=development

# === Database ===
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/agentgov"
DB_POOL_MAX=20                    # Max connections in pool
DB_POOL_IDLE_TIMEOUT=30000        # Idle connection timeout (ms)

# === Redis ===
REDIS_URL="redis://:redis_dev_password@localhost:6379"
REDIS_PASSWORD=redis_dev_password  # Must match docker-compose

# === API ===
API_PORT=3001
API_URL="http://localhost:3001"

# === Web ===
NEXT_PUBLIC_API_URL="http://localhost:3001"
NEXT_PUBLIC_WS_URL="ws://localhost:3001/ws"
WEB_URL="http://localhost:3000"

# === Auth ===
BETTER_AUTH_URL="http://localhost:3001"
BETTER_AUTH_SECRET=""              # openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"

# === Security ===
ALLOWED_ORIGINS="http://localhost:3000,http://localhost:3001"  # Comma-separated
RATE_LIMIT_GLOBAL=100             # Requests per minute (global)
RATE_LIMIT_AUTH=30                # Requests per minute (auth endpoints)
WS_TICKET_CHECK_IP=false          # Bind WS ticket to client IP (requires trustProxy behind CDN)

# === OAuth Providers (optional) ===
# Google OAuth: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# GitHub OAuth: https://github.com/settings/developers
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# === Email Service (Resend) ===
# Get API key at https://resend.com/api-keys
RESEND_API_KEY=""
EMAIL_FROM="noreply@yourdomain.com"

# === Stripe (optional) ===
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
STRIPE_PUBLISHABLE_KEY=""

# === Sentry (optional) ===
# Get DSN at https://sentry.io
SENTRY_DSN=""
NEXT_PUBLIC_SENTRY_DSN=""
SENTRY_AUTH_TOKEN=""
SENTRY_ENVIRONMENT="development"
NEXT_PUBLIC_SENTRY_ENVIRONMENT="development"

# === Telegram Notifications (optional) ===
# Sends real-time notifications about new users and feedback
#
# Setup:
# 1. Create bot via @BotFather on Telegram
# 2. Send /start to your bot
# 3. Get chat_id: curl https://api.telegram.org/bot<TOKEN>/getUpdates
#
# SECURITY: Never commit real tokens. Regenerate if exposed.
# @see https://core.telegram.org/bots/api
TELEGRAM_BOT_TOKEN=""
TELEGRAM_CHAT_ID=""

# === Docker ===
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres         # CHANGE IN PRODUCTION
POSTGRES_DB=agentgov

# ⚠️ SECURITY NOTES:
# - NEVER commit .env files with real credentials
# - Rotate BETTER_AUTH_SECRET immediately if exposed
# - Use separate credentials for production
# - REDIS_PASSWORD and POSTGRES_PASSWORD must match docker-compose values
